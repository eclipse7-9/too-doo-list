name: Disabled CI Build (kept for reference)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is disabled. Use deploy-gh-pages.yml for deployment." 
