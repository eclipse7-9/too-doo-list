name: Disabled deploy workflow (kept for reference)

# This workflow was disabled to avoid duplicated GitHub Pages deployments.
# It can be triggered manually if needed.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is disabled. Use deploy-gh-pages.yml instead."